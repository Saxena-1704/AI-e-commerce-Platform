// Single frontend configuration point.
// Change this when the backend moves from local development to deployment.
export const API_BASE_URL = "http://localhost:8000";

export const CONFIG = {
  API_BASE_URL,
};
